# boilerplate-docker-express-server

### Available commmands

Be sure to install Docker on machine before running the commands in below.

To start up the app:
```
docker-compose up
```

or without Docker:

```
yarn serve
```